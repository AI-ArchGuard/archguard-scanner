# ArchGuard 工作约定

## 工作原则

1. 修改前先阅读目标仓库的 README、AGENTS、相关需求、设计和ADR。
2. 先检查现有代码、测试和工作树；不得覆盖用户已有修改。
3. 一次任务只解决一个明确问题，不顺带大规模重构。
4. 先给出实施计划、影响范围和风险，再修改代码。
5. 优先复用现有抽象；新增依赖必须说明理由、许可证和替代方案。
6. 禁止提交密钥、Token、密码、真实客户源码或个人数据。
7. 禁止为了让测试通过而删除测试、降低断言或吞掉异常。
8. 修改后运行最小相关测试，再运行仓库完整质量检查。
9. 未实际运行的验证必须明确标注，不得声称已经通过。
10. 跨仓库变更必须列出兼容顺序、版本和回滚方案。

## 架构限制

- `archguard-platform` 初期保持模块化单体，不因“展示微服务”而拆分。
- 领域模块不得直接访问其他模块的数据库表或内部实现。
- 领域层不得依赖 Spring Web、数据库驱动或外部模型SDK。
- Scanner输出稳定的版本化契约，不依赖Platform内部类。
- MCP工具默认只读、最小权限；写操作必须显式审批。
- LLM输出是不可信输入，必须做Schema、权限和业务校验。
- 确定性规则是事实基础，LLM负责解释、归纳与低风险建议。
- 数据库、Redis和内部服务端口不得暴露到公网。

## 代码要求

- Java：构造器注入；禁止字段注入；公共边界使用明确DTO；事务边界放在应用层。
- Go：显式错误处理；所有外部调用接受 `context.Context`；避免Goroutine泄漏。
- API：错误结构统一；分页、幂等、版本和兼容策略明确。
- 数据库：Schema变更必须使用迁移脚本；已发布迁移不得修改。
- 日志：结构化输出，包含 traceId；禁止记录凭据和完整源码。
- 测试：修复缺陷必须先增加能够复现问题的测试。

## 输出格式

每次完成任务时报告：

1. 完成了什么；
2. 修改了哪些文件；
3. 关键架构选择；
4. 执行了哪些测试及结果；
5. 已知限制和下一步。

## 仓库专属边界

- 扫描结果必须确定、可重复，并通过显式版本化 Schema 发布。
- 禁止依赖 Platform 内部类、业务表或运行时实现。
- 确定性规则是事实基础；不得引入 LLM 作为违规判定器。
- 解析失败要返回可定位错误；源码仅按最小需要处理并明确清理。

## 验证命令

- 当前基线：`git diff --check`、`git status --short`。
- Maven Wrapper 建立后：先运行相关黄金测试，再运行 `./mvnw verify`；Windows 使用 `.\mvnw.cmd verify`。
- 契约变化必须运行跨仓库契约测试，并记录兼容版本、发布顺序和回滚方案。
