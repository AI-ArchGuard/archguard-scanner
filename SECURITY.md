# Security Policy

## 支持范围

项目处于 `0.x` 开发阶段，仅当前默认分支和最新发布版本接收安全修复。

## 报告漏洞

请使用 GitHub Security Advisories 的私密漏洞报告功能。不要在公开 Issue、讨论、日志或示例中披露可利用细节、凭据、真实源码或个人数据。

报告应尽量包含受影响组件和版本、复现条件、实际与预期结果、影响评估，以及不含敏感数据的最小证据。

## 安全基线

- 所有凭据只通过受控环境或 Secret Manager 注入。
- 外部输入和 LLM 输出必须通过 Schema、权限和业务规则校验。
- 写操作必须显式授权；MCP 工具默认只读、最小权限。
- 安全修复不得通过吞掉异常、降低断言或删除测试完成。

项目维护者会在完成初步分级后，通过私密渠道协调修复、验证和披露。
